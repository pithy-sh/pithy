---
"@pithy-sh/secrets": minor
"@pithy-sh/cloudflare": minor
---

New package: `@pithy-sh/secrets` — per-environment secret storage (CF Secrets Store or encrypted D1) with a worker-only master key, a singleton secrets manager per environment, automatic at-rest key rotation, and an environment-aware `pithy secrets` CLI that routes global secrets to every environment.

# deps & build
node_modules/
dist/

# cloudflare / wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# pithy local dev state
.dev-state.json
.dev-ports.json
logs/

# pithy feature worktrees + per-feature state
.worktrees/
.dev.config.json
.pithy-feature.json

# env & secrets
.env
.env.*
!.env.example

# atomic-write temp siblings (.dev.vars.<rand>.tmp, .dev.secrets.jsonc.<rand>.tmp, …). A write
# interrupted by SIGINT leaves one holding the full plaintext; it must never be committable.
*.tmp

# editor / os
.DS_Store
*.log
coverage/

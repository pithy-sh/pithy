# deps & build
node_modules/
dist/

# cloudflare / wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# pithy local dev state
.dev-state.json
.dev-ports.json
logs/

# pithy feature worktrees + per-feature state
.worktrees/
.dev.config.json
.pithy-feature.json

# env & secrets
.dev.secrets.jsonc
# The atomic write's temp file. `.dev.vars.*` covers its sibling; nothing covered this one, and a
# SIGINT between the write and the rename leaves it behind holding full plaintext.
.dev.secrets.jsonc.tmp
!.dev.secrets.example.jsonc
.env
.env.*
!.env.example

# editor / os
.DS_Store
*.log
coverage/

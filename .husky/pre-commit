# --concurrent false: the license-header task rewrites the same staged files Biome reads, and
# lint-staged runs different globs' tasks in parallel by default. See lint-staged.config.js.
bunx lint-staged --concurrent false

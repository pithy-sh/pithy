# deps & build
node_modules/
dist/
.turbo/

# feature worktrees (scripts/worktree.ts)
.worktrees/

# cloudflare / wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# env & secrets
.env
.env.*
!.env.example

# local-only notes and scratch (never committed)
/.local/

# CLI test scaffolds (removed after each run; crash residue stays out)
.smoke-*/
.e2e-*/
.migrate-*/

# editor / os
.DS_Store
*.log
coverage/
